@model TriThucOnline_TTN.Models.Book

<table class="table table-bordered table-hover table-condensed table-striped">
    <thead>
        <tr>
            <th>Mã sách:</th>
            <th>Tên sách:</th>
            <th>Giá bán:</th>
            <th>Mô tả:</th>
            <th>Ngày cập nhật:</th>
            <th>Ảnh bìa:</th>
            <th>Số lượng tồn:</th>
            <th>Chủ đề:</th>
            <th>Nhà xuất bản:</th>
            <th>Tác giả:</th>
        </tr>
    </thead>
    <tbody>

        <tr>
            <td>@model.id</td>
            <td>@model.title</td>
            <td>@string.Format("{0:0,0}", model.price) VNĐ</td>
            @if (model.quotes_about.Length <= 140)
            {
                <td>@model.quotes_about </td>
            }
            else
            {
                <td>@model.quotes_about.Substring(0, 140) ...</td>
            }
            <td>@model.publish_year </td>
            <td>
                <img src="@model.picture" class="img-responsive" style="width: 150px; height: auto"/>
            </td>
            <td>@model.quantity</td>
            <td>@model.category_name </td>
            <td>@model.publisher_name </td>
            <td>@model.author_name </td>

        </tr>

    </tbody>
</table>
